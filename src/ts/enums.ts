/* eslint-disable @/no-unused-vars */
/* eslint-disable no-unused-vars */

enum Direction {
  Up,
  Down,
  None,
}

enum State {
  Wait,
  Move,
}

enum LookDirection {
  Left,
  Right,
}

export { Direction, State, LookDirection };
